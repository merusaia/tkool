作者：　メルサイア
デフォルトのプラグインrpg_***.jsから、
変更を加えた行に「★」をつけて、列挙します。

■rpg_object.js：　クラス宣言系（Game_Battlerなど）
(1)変更箇所
なし
(2)コメントを追加した箇所
なし

■rpg_sprites.js：　描画処理系
(1)変更箇所
なし

(2)コメント「//」だけ追加した箇所
あり

コメントを追加した箇所（使用しているプラグインの説明や、競合しないようにするための注意書き）

・567行目～：
Sprite_Battler.prototype.setupDamagePopup = function() {
    if (this._battler.isDamagePopupRequested()) {
        if (this._battler.isSpriteVisible()) {
            var sprite = new Sprite_Damage();
            sprite.x = this.x + this.damageOffsetX();
            sprite.y = this.y + this.damageOffsetY();
            sprite.setup(this._battler);
            this._damages.push(sprite);   // merusaia_MyBattleプラグインでは、ここをコメントアウトすると、ダメージポップアップを消せる→ここだけじゃ消せない。次の行も消さないといけないよ。（デフォルトは、表示）
            this.parent.addChild(sprite); // merusaia_MyBattleプラグインでは、ここをコメントアウトすると、ダメージポップアップを消せる→上のと合わせて２行消す。
        }
        this._battler.clearDamagePopup();
        this._battler.clearResult(); // merusaia_MyBattleプラグインでは、回避したかをコモンイベントで取得するためにここを削除。トリアコンタンさんに感謝。（デフォルトは、ここで戦闘結果をクリアするみたい）
    }
};


・1507行目～：
Sprite_Damage.prototype.setup = function(target) { // minRPG_CriticalEffectでは、クリティカル演出を変更可能にするため上書き。（デフォルトは、赤くフラッシュするだけ）
    var result = target.result();
    if (result.missed || result.evaded) {
        this.createMiss();
    } else if (result.hpAffected) {
        this.createDigits(0, result.hpDamage);
    } else if (target.isAlive() && result.mpDamage !== 0) {
        this.createDigits(2, result.mpDamage);
    }
    if (result.critical) {
        this.setupCriticalEffect();
    }
};

Sprite_Damage.prototype.setupCriticalEffect = function() { // minRPG_CriticalEffectでは、デフォルトのクリティカル演出は使ってないよ。上書きはしていないよ。
    this._flashColor = [255, 0, 0, 160];
    this._flashDuration = 60;
};
